<h1>Wines</h1>
<p>Available as of <%= formatted_time(@available_at) %></p>
<ol>
  <% @wines.each do |wine| %>
    <li>
      <%= link_to(wine.name , wine) %>
      by <%= wine.winery + " (" + wine.varietal  + ")" %>
    </li>
  <% end %>
</ol>
<br />
<%= link_to 'New Wine', new_wine_path %>
</ol>